import 'package:flutter/material.dart';

Icon cross =
    const Icon(Icons.clear, color: Color.fromRGBO(0, 188, 212, 1), size: 60);
Icon circle = const Icon(Icons.circle_outlined,
    color: Color.fromRGBO(229, 57, 53, 1), size: 60);

enum Symbol { cross, circle, none }
